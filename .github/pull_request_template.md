## What does this PR do?

<!-- One or two sentences. If it fixes a bug, say what the bug was. If it adds a feature, say what problem it solves. -->

## Related issue

<!-- Link the issue this closes, e.g. `Closes #12`. If there is none, say so and explain why. -->

## How was it tested?

<!--
  What did you run? Minimum is `pnpm check` (format + lint + full suite).
  If you touched src/model/, run `pnpm test:provider` too.
-->

- [ ] `pnpm check` passes
- [ ] `pnpm test:provider` passes (only if `src/model/` changed)
- [ ] Added or updated tests in `test/`

## Notes for reviewers

<!-- Anything the reviewer should pay attention to, design decisions you made, or parts you're unsure about. -->
