diff --git a/node_modules/@earendil-works/pi-tui/dist/autocomplete.js b/node_modules/@earendil-works/pi-tui/dist/autocomplete.js
index 4c3c64c..a1b2c3d 100644
--- a/node_modules/@earendil-works/pi-tui/dist/autocomplete.js
+++ b/node_modules/@earendil-works/pi-tui/dist/autocomplete.js
@@ -195,12 +195,21 @@
                 isQuotedPrefix,
                 signal: options.signal,
             });
-            if (suggestions.length === 0)
-                return null;
-            return {
-                items: suggestions,
-                prefix: atPrefix,
-            };
+            if (suggestions.length > 0) {
+                return {
+                    items: suggestions,
+                    prefix: atPrefix,
+                };
+            }
+            // Fallback to synchronous file listing when fd is not available
+            const fallbackSuggestions = this.getFileSuggestions(atPrefix);
+            if (fallbackSuggestions.length > 0) {
+                return {
+                    items: fallbackSuggestions,
+                    prefix: atPrefix,
+                };
+            }
+            return null;
         }
         if (!options.force && textBeforeCursor.startsWith("/")) {
             const spaceIndex = textBeforeCursor.indexOf(" ");
